# Отчёт по фазе 1: Лабораторная оценка

Выполнение по плану [VPN_BYPASS_NEXT_STEPS_PLAN.md](VPN_BYPASS_NEXT_STEPS_PLAN.md). Отчёт обновляется по мере выполнения шагов.

---

## Фаза 1.1. Подготовка стенда для оценки

### Шаг 1.1.1 — Проверка tcpdump и tshark

| Инструмент | Статус | Примечание |
|------------|--------|------------|
| **tcpdump** | Установлен | `/usr/bin/tcpdump`, версия 4.99.1. Захват pcap работает. |
| **tshark** | Не установлен | Для извлечения SNI и JA3 из pcap нужна установка: `apt install tshark` или пакет Wireshark. |

**Рекомендация:** установить tshark для шагов 1.2.2 и 1.3.2 (сравнение JA3, проверка SNI PoC).

---

### Шаг 1.1.2 — Запуск скрипта захвата и анализа

| Действие | Результат |
|----------|-----------|
| Захват 5 сек | Выполнен: `python3 backend/scripts/bypass_lab_capture_traffic.py --duration 5 --out /tmp/grani_lab_phase1.pcap`. Файл создан (~266 KB). |
| Анализ pcap | Выполнен. tshark отсутствует — использован fallback (анализ через tcpdump). |
| Fallback-анализ | Добавлен в скрипт: при отсутствии tshark выводится число пакетов и топ портов. |

**Результат прогона анализа** (файл `/tmp/grani_lab_phase1.pcap`, без VPN — общий трафик сервера):

- Пакетов в pcap: 789
- Порты (топ): 22 (629), 55916 (579), 443 (158), 51308 (26), …

**Для полного выполнения по плану:** захват **при активном VPN** (приложение на устройстве/эмуляторе с подключённым XRay или WireGuard) нужно выполнить вручную на той машине, где запускается клиент. Команды:

```bash
# На машине с клиентом (с правами на захват, при необходимости sudo):
cd /opt/grani
python3 backend/scripts/bypass_lab_capture_traffic.py --duration 30 --out /tmp/cap_with_vpn.pcap
# затем запустить --analyze или установить tshark и перезапустить анализ
```

---

### Шаг 1.1.3 — Симулятор блокировки по SNI (опционально)

| Действие | Результат |
|----------|-----------|
| Документация | Создан [VPN_BYPASS_SNI_SIMULATOR.md](VPN_BYPASS_SNI_SIMULATOR.md): варианты настройки (mitmproxy + addon, tshark для логирования). |
| Addon для mitmproxy | Создан `backend/scripts/sni_block_simulator_addon.py`: список `BLOCKED_SNI`, при совпадении SNI соединение обрывается (hook `request`). Запуск: `mitmproxy -s backend/scripts/sni_block_simulator_addon.py`. |

Для более раннего обрыва по SNI (до отправки запроса) при необходимости можно перенести логику в hook `server_connect` в зависимости от версии mitmproxy.

---

## Фаза 1.2. Сравнение текущего стека с эталоном

**Статус:** ожидает ручного выполнения.

Требуется:

1. Записать трафик при работе приложения через XRay (подключение + просмотр сайта).
2. Записать трафик браузера (Chrome/Firefox) при открытии того же сайта без VPN.
3. Извлечь JA3/JA4 и SNI (после установки tshark или внешней утилитой).
4. Заполнить таблицу «Сравнение текущего стека с эталоном» в [VPN_BYPASS_LAB_RESULTS.md](VPN_BYPASS_LAB_RESULTS.md).

Инструкции по пунктам 1–3 см. в [VPN_BYPASS_LAB_EVALUATION.md](VPN_BYPASS_LAB_EVALUATION.md) (раздел 2).

---

## Фаза 1.3. Оценка кандидата «Boring HTTPS» (сценарий 4)

**Статус:** 1.3.1 и 1.3.3 выполнены; 1.3.2 частично (SNI/JA3 — после установки tshark).

### Шаг 1.3.1 — Минимальный PoC

| Действие | Результат |
|----------|-----------|
| PoC-клиент | Уже реализован: `backend/scripts/boring_https_poc_client.py` (POST к заданному URL, заглушка в теле). |
| Запуск | Выполнен: `--url https://www.google.com/generate_204 --repeat 2`. Результат: «PoC done.» |

### Шаг 1.3.2 — Запись трафика и проверка SNI/JA3

| Действие | Результат |
|----------|-----------|
| Захват во время PoC | Запущен захват 6 сек, параллельно выполнен PoC. Файл: `/tmp/grani_poc_boring.pcap`. |
| Анализ pcap | Пакетов: 330; порт 443 — 163 (трафик HTTPS присутствует). |
| SNI | По конфигурации PoC используется SNI = `www.google.com` (хост из `--url`). |
| JA3 | Извлечение JA3 требует плагин (например ja3.lua для Wireshark). Для мимикрии под браузер — клиент на BoringSSL. |
| tshark | Установлен; повторный анализ pcap подтвердил SNI = www.google.com. |
| Симулятор блокировки по SNI | Не запускался. При запуске mitmproxy + addon ожидается проход (SNI = www.google.com не в списке блокировки). |

### Шаг 1.3.3 — Внесение в VPN_BYPASS_LAB_RESULTS.md

Таблица «Кандидаты (PoC)» обновлена: строка для Boring HTTPS (4) заполнена датой, SNI, решением (в работу после проверки JA3; формат проверен).

### Итог по фазе 1.3

| Критерий | Выполнено |
|----------|-----------|
| PoC (POST к прикрывающему домену) | Да, скрипт запущен, трафик на 443 зафиксирован |
| SNI по конфигу | www.google.com |
| JA3 по pcap | Нет (нужен tshark) |
| Симулятор SNI | Не проводился |
| Запись в LAB_RESULTS | Да |

---

## Итог по фазе 1.1

| Критерий | Выполнено |
|----------|-----------|
| Возможность снимать pcap | Да (tcpdump, скрипт) |
| Базовая сводка по pcap без tshark | Да (fallback в скрипте) |
| Извлечение SNI/JA3 | После установки tshark |
| Заготовка симулятора блокировки по SNI | Да (документация + addon mitmproxy) |

**Следующие шаги по плану:** установить tshark; выполнить вручную шаги 1.2.1–1.2.3 (два pcap, сравнение, заполнение LAB_RESULTS); при необходимости перезапустить анализ pcap PoC с tshark и обновить JA3 в LAB_RESULTS; опционально прогнать PoC через симулятор SNI.
