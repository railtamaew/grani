# Отчет об очистке зависимостей

**Дата:** 2026-01-04
**Резервная копия:** backups/dependencies_20260104_002107

## Удаленные зависимости

### Admin Panel (package.json)

Удалено 3 неиспользуемые зависимости:
- ❌ `react-hook-form` (^7.48.2) - не найдено использований
- ❌ `yup` (^1.3.3) - не найдено использований
- ❌ `date-fns` (^2.30.0) - не найдено использований

**Сохранено:** `web-vitals` - может использоваться для метрик производительности

### Mobile App (pubspec.yaml)

Удалено 5 неиспользуемых зависимостей:
- ❌ `sqflite` (^2.4.2) - не найдено использований (используется только shared_preferences)
- ❌ `hive` (^2.2.3) - не найдено использований
- ❌ `hive_flutter` (^1.1.0) - не найдено использований
- ❌ `basic_utils` (^5.7.0) - не найдено использований
- ❌ `hive_generator` (^2.0.1) - dev зависимость для hive (не нужна)

### Backend (requirements.txt)

Все зависимости используются:
- ✅ Все зависимости оставлены (используются в коде)

## Потенциальная экономия

- **Admin Panel:**
  - Уменьшение размера `node_modules`: ~50-100MB
  - Ускорение установки: `npm install` будет быстрее

- **Mobile App:**
  - Уменьшение размера APK: ~5-10MB
  - Ускорение сборки: меньше зависимостей для компиляции

## Восстановление

При необходимости можно восстановить из резервной копии:
```bash
cp backups/dependencies_20260104_002107/package.json.bak admin-panel/package.json
cp backups/dependencies_20260104_002107/pubspec.yaml.bak mobile-app/pubspec.yaml
```

## Следующие шаги

1. Переустановить зависимости:
   ```bash
   cd admin-panel && npm install
   cd ../mobile-app && flutter pub get
   ```

2. Проверить, что проект собирается без ошибок

3. Убедиться, что функциональность не нарушена

