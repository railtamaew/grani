# Анализ файлов в корне проекта

**Дата:** 2026-01-04

## Критичные файлы (НУЖНЫ для работы проекта)

### Скрипты сборки:
- ✅ `build_release_apk.sh` - основной скрипт сборки APK

### Конфигурация:
- ✅ `.gitignore` - для Git (если есть)
- ✅ `.env*` - переменные окружения (если есть)

## Файлы, которые можно оптимизировать

### 1. Дубликаты скриптов установки (можно удалить):
- ❌ `auto_install_apk.sh`
- ❌ `do_install.sh`
- ❌ `install_and_run.sh`
- ❌ `install_with_output.sh`
- ❌ `quick_install.sh`
- ❌ `run_install_now.sh`
- ❌ `simple_install.sh`
- ✅ Оставить только: `INSTALL_APK.sh` или `install_apk.py`

### 2. Дубликаты скриптов сборки (можно удалить):
- ❌ `build_apk_interactive.sh`
- ❌ `diagnose_build.sh`
- ❌ `monitor_build.sh`
- ✅ Оставить только: `build_release_apk.sh`

### 3. Дубликаты скриптов ADB (можно удалить):
- ❌ `setup_adb_local.sh`
- ❌ `setup_adb_network.sh`
- ❌ `setup_adb_ssh_tunnel.sh` (дубликат)
- ✅ Оставить только: `connect_remote_device.sh` (если используется)

### 4. Отчеты (можно переместить в docs/reports/):
- Все `*REPORT*.md` файлы
- Все `*FIX*.md` файлы
- Все `*SUMMARY*.md` файлы
- Все `CLEANUP_REPORT*.md` файлы
- Все `OPTIMIZATION*.md` файлы

### 5. Инструкции (можно переместить в docs/):
- Все `*INSTRUCTIONS*.md` файлы
- Все `*SETUP*.md` файлы
- Все `*HOW*.md` файлы
- Все `*QUICK*.md` файлы

### 6. Временные файлы (можно удалить):
- ❌ `granivpn-release.apk` - можно пересобрать
- ❌ `*.ps1` файлы - для Windows, не нужны на Linux
- ❌ `disable_cursor_sound.sh` - временный скрипт

### 7. Утилиты (можно переместить в scripts/):
- `ai_helper_install.sh`
- `start_download_server.sh`
- `download_apk.sh`

## Рекомендации

### Высокий приоритет:
1. **Удалить дубликаты скриптов** - оставить только рабочие версии
2. **Переместить отчеты** в `docs/reports/`
3. **Переместить инструкции** в `docs/`
4. **Удалить .apk файлы** - можно пересобрать

### Средний приоритет:
1. **Переместить утилиты** в `scripts/`
2. **Удалить .ps1 файлы** (Windows скрипты)

### Низкий приоритет:
1. **Организовать документацию** по категориям

## Потенциальная экономия

- Удаление дубликатов: ~10-20 файлов
- Перемещение документации: ~40-50 файлов
- Очистка корня проекта: более чистая структура

## Итог

**Критичные файлы:** ~1-3 файла
**Можно оптимизировать:** ~60-70 файлов

